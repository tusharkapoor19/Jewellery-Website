# Optional. Without these set, the app falls back to a local data-URL
# so image upload + AI price estimator still work fully offline.
REACT_APP_CLOUDINARY_CLOUD_NAME=your_cloud_name
REACT_APP_CLOUDINARY_UPLOAD_PRESET=your_unsigned_preset
